{
  "name": "wildbus-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && touch ../docs/.nojekyll"
  },
  "devDependencies": {
    "vite": "^6.0"
  }
}
